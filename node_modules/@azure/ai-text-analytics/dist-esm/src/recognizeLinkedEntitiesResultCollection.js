// Copyright (c) Microsoft Corporation.
// Licensed under the MIT license.
import { makeRecognizeLinkedEntitiesResult, makeRecognizeLinkedEntitiesErrorResult } from "./recognizeLinkedEntitiesResult";
import { sortByPreviousIdOrder } from "./util";
export function makeRecognizeLinkedEntitiesResultCollection(input, documents, errors, modelVersion, statistics) {
    const unsortedResult = documents
        .map((document) => {
        return makeRecognizeLinkedEntitiesResult(document.id, document.entities, document.statistics);
    })
        .concat(errors.map((error) => {
        return makeRecognizeLinkedEntitiesErrorResult(error.id, error.error);
    }));
    const result = sortByPreviousIdOrder(input, unsortedResult);
    return Object.assign(result, {
        statistics,
        modelVersion
    });
}
//# sourceMappingURL=recognizeLinkedEntitiesResultCollection.js.map