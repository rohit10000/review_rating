// Copyright (c) Microsoft Corporation.
// Licensed under the MIT license.
import { makeRecognizeCategorizedEntitiesResult, makeRecognizeCategorizedEntitiesErrorResult } from "./recognizeCategorizedEntitiesResult";
import { sortByPreviousIdOrder } from "./util";
export function makeRecognizeCategorizedEntitiesResultCollection(input, documents, errors, modelVersion, statistics) {
    const unsortedResult = documents
        .map((document) => {
        return makeRecognizeCategorizedEntitiesResult(document.id, document.entities, document.statistics);
    })
        .concat(errors.map((error) => {
        return makeRecognizeCategorizedEntitiesErrorResult(error.id, error.error);
    }));
    const result = sortByPreviousIdOrder(input, unsortedResult);
    return Object.assign(result, {
        statistics,
        modelVersion
    });
}
//# sourceMappingURL=recognizeCategorizedEntitiesResultCollection.js.map