// Copyright (c) Microsoft Corporation.
// Licensed under the MIT license.
import { makeRecognizePiiEntitiesResult, makeRecognizePiiEntitiesErrorResult } from "./recognizePiiEntitiesResult";
import { sortByPreviousIdOrder } from "./util";
export function makeRecognizePiiEntitiesResultCollection(input, documents, errors, modelVersion, statistics) {
    const unsortedResult = documents
        .map((document) => {
        return makeRecognizePiiEntitiesResult(document.id, document.entities, document.statistics);
    })
        .concat(errors.map((error) => {
        return makeRecognizePiiEntitiesErrorResult(error.id, error.error);
    }));
    const result = sortByPreviousIdOrder(input, unsortedResult);
    return Object.assign(result, {
        statistics,
        modelVersion
    });
}
//# sourceMappingURL=recognizePiiEntitiesResultCollection.js.map