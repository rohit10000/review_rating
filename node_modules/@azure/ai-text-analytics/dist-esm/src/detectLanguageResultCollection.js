// Copyright (c) Microsoft Corporation.
// Licensed under the MIT license.
import { makeDetectLanguageResult, makeDetectLanguageErrorResult } from "./detectLanguageResult";
import { sortByPreviousIdOrder } from "./util";
export function makeDetectLanguageResultCollection(input, documents, errors, modelVersion, statistics) {
    const unsortedResult = documents
        .map((document) => {
        return makeDetectLanguageResult(document.id, document.detectedLanguages, document.statistics);
    })
        .concat(errors.map((error) => {
        return makeDetectLanguageErrorResult(error.id, error.error);
    }));
    const result = sortByPreviousIdOrder(input, unsortedResult);
    return Object.assign(result, {
        statistics,
        modelVersion
    });
}
//# sourceMappingURL=detectLanguageResultCollection.js.map