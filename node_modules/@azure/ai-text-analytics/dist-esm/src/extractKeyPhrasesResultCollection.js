// Copyright (c) Microsoft Corporation.
// Licensed under the MIT license.
import { makeExtractKeyPhrasesResult, makeExtractKeyPhrasesErrorResult } from "./extractKeyPhrasesResult";
import { sortByPreviousIdOrder } from "./util";
export function makeExtractKeyPhrasesResultCollection(input, documents, errors, modelVersion, statistics) {
    const unsortedResult = documents
        .map((document) => {
        return makeExtractKeyPhrasesResult(document.id, document.keyPhrases, document.statistics);
    })
        .concat(errors.map((error) => {
        return makeExtractKeyPhrasesErrorResult(error.id, error.error);
    }));
    const result = sortByPreviousIdOrder(input, unsortedResult);
    return Object.assign(result, {
        statistics,
        modelVersion
    });
}
//# sourceMappingURL=extractKeyPhrasesResultCollection.js.map