// Copyright (c) Microsoft Corporation.
// Licensed under the MIT license.
import { makeAnalyzeSentimentResult, makeAnalyzeSentimentErrorResult } from "./analyzeSentimentResult";
import { sortByPreviousIdOrder } from "./util";
export function makeAnalyzeSentimentResultCollection(input, documents, errors, modelVersion, statistics) {
    const unsortedResult = documents
        .map((document) => {
        return makeAnalyzeSentimentResult(document.id, document.sentiment, document.documentScores, document.sentenceSentiments, document.statistics);
    })
        .concat(errors.map((error) => {
        return makeAnalyzeSentimentErrorResult(error.id, error.error);
    }));
    const result = sortByPreviousIdOrder(input, unsortedResult);
    return Object.assign(result, {
        statistics,
        modelVersion
    });
}
//# sourceMappingURL=analyzeSentimentResultCollection.js.map