// Copyright (c) Microsoft Corporation.
// Licensed under the MIT license.
import { makeTextAnalyticsSuccessResult, makeTextAnalyticsErrorResult } from "./textAnalyticsResult";
export function makeDetectLanguageResult(id, detectedLanguages, statistics) {
    return Object.assign(Object.assign({}, makeTextAnalyticsSuccessResult(id, statistics)), { primaryLanguage: primaryLanguage(detectedLanguages) });
}
export function makeDetectLanguageErrorResult(id, error) {
    return makeTextAnalyticsErrorResult(id, error);
}
function primaryLanguage(languages) {
    const sorted = languages.slice(0).sort((a, b) => {
        return a.score - b.score;
    });
    return sorted[0];
}
//# sourceMappingURL=detectLanguageResult.js.map